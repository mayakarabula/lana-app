rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single shared progress doc (see SHARED_PROGRESS_DOC_ID in useProgress.ts).
    match /progress/{docId} {
      allow read, write: if request.auth != null && docId == 'lana';
    }
  }
}

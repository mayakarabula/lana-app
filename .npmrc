audit=false
fund=false
# Use the public registry so CI (e.g. Netlify) and collaborators can npm ci without VPN.
registry=https://registry.npmjs.org/
# Netlify sets NODE_ENV=production during install; without this, devDependencies are skipped.
production=false
